export const getEvents = (state) => state.mediaReducer.eventList;
export const getSocials = (state) => state.mediaReducer.socialList;
export const getGalleries = (state) => state.mediaReducer.galleriesList;
